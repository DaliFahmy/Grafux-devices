fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
# anthropic >= 0.69 is required for the remote-MCP connector used by claws
# (client.beta.messages with mcp_servers + the "mcp-client-2025-11-20" beta).
anthropic>=0.69
# httpx powers the Composio REST calls in openclaw/connections.py. It is not pulled
# in transitively by the base server, so it is pinned here (Render installs this file).
httpx>=0.23
# mcp is the Model Context Protocol SDK used by openclaw/connections.run_local_agent_loop
# to drive header-authenticated MCP servers (Composio Connect/Tool-Router, x-consumer-api-key)
# that Anthropic's bearer-only connector cannot reach.
mcp>=1.0.0
# segno is a pure-python QR encoder (no Pillow/native deps) used by openclaw/qr.py to render
# the claw's "qr_code" port — a scannable QR of an app's authorization link.
segno>=1.5.2
# paramiko powers the SSH compile-and-run loop in GPU/runpod_client.py (httpx above
# is reused for the RunPod REST API). Pinned here because Render installs this file.
paramiko>=3.0
