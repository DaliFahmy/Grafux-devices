# Device-side WebSocket server dependencies.
# Compilers/interpreters (gcc, g++, python3, bash) come from the OS, not pip.
websockets>=11.0

# Optional: LAN mDNS discovery so the Grafux app finds this device automatically.
# The server runs fine without it (advertisement is skipped if absent).
zeroconf>=0.130
